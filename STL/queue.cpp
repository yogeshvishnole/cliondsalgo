#include<iostream>
#include<queue>
using namespace std;


/*
 *  Queue
 *
 *   Container adapter with the functionality of a queue
 *
 *   Need to include <queue> header file
 *
 *   The class template acts as a wrapper to the underlying
 *   container - only a specific set of functions is provided
 *
 *   Pushes elements at one end (front) and pops from other end (back) of underlying contianer
 *
 *   *  Stack functions
 *  1. size
 *  2. =
 *  3. push(),pop()
 *  4. empty()
 *  5. front(),back()
 *
 * */

int main(){

}