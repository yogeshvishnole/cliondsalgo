#include <iostream>

int main()
{
        // Array in c++ is a collection of values of same type stored in contiguos block of memory

        // Array declaration

//	data_type var_name[size_must];

        int arr[5];

        arr[0] = 1;

        int temp[10] = {1,2};

//	if we initialize one value of array all values are automatically initialized to 0;

        // After this we solve following problems

        /*
         * 1. sum of elements of array
         * 2. swap of two numbers
         * 3. reverse the array
         * 4. Segmentation fault in array
         *
         * */
}
